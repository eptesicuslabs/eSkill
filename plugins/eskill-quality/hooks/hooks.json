{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "^(Write|Edit)$",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if the code just written contains any obvious security issues: hardcoded secrets (API keys, passwords, tokens), SQL injection (string concatenation in queries), command injection (unsanitized input in shell commands), or path traversal (unsanitized input in file paths). If you find a clear security issue, respond with a JSON object: {\"continue\": true, \"systemMessage\": \"Security warning: [brief description of the issue and which line]\"}. If the code looks safe, respond with {\"continue\": true}.",
            "model": "haiku"
          }
        ]
      },
      {
        "matcher": "^Write$",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if the Write tool is creating a file matching sensitive patterns: .env, *.pem, *.key, *credentials*, *secret*. Also check if the file content exceeds 10KB which may indicate accidental binary or data dump writes. If the file matches a sensitive pattern, respond BLOCK with a warning explaining that sensitive files should not be written to the repository. If the file is oversized, respond BLOCK with a note about the file size. If safe, respond ALLOW.",
            "model": "haiku"
          }
        ]
      }
    ]
  }
}
